= form_for @applicant, remote: true, html: { role: "form" } do |f|
  .form-group{class: ("has-error" if @applicant.errors.messages[:name].present?)}
    = f.label :name, raw("<abbr title='required'>*</abbr> Full Name"), class: "control-label" if ENV["FORM_LABELS"]
    = f.text_field :name, class: "form-control", placeholder: "Full Name *"
    - if @applicant.errors.messages[:name].present?
      %p.help-block= @applicant.errors.messages[:name][0]

  .form-group{class: ("has-error" if @applicant.errors.messages[:email].present?)}
    = f.label :email, raw("<abbr title='required'>*</abbr> Email"), class: "control-label" if ENV["FORM_LABELS"]
    = f.email_field :email, class: "form-control", placeholder: "Email Address *"
    - if @applicant.errors.messages[:email].present?
      %p.help-block= @applicant.errors.messages[:email][0]

  .form-group{class: ("has-error" if @applicant.errors.messages[:school].present?)}
    = f.label :email, raw("<abbr title='required'>*</abbr> School Where Attending/Graduated"), class: "control-label" if ENV["FORM_LABELS"]
    = f.text_field :school, class: "form-control", placeholder: "School Where Attending/Graduated *"
    - if @applicant.errors.messages[:school].present?
      %p.help-block= @applicant.errors.messages[:school][0]

  .row 
    .col-sm-6
      .form-group{class: ("has-error" if @applicant.errors.messages[:balance].present?)}
        = f.label :email, raw("<abbr title='required'>*</abbr> Currrent Loan Balance"), class: "control-label" if ENV["FORM_LABELS"]
        = f.text_field :balance, class: "form-control currency", placeholder: "Currrent Loan Balance *"
        - if @applicant.errors.messages[:balance].present?
          %p.help-block= @applicant.errors.messages[:balance][0]

    .col-sm-6
      .form-group{class: ("has-error" if @applicant.errors.messages[:rate].present?)}
        = f.label :email, raw("<abbr title='required'>*</abbr> Current Interest Rate"), class: "control-label" if ENV["FORM_LABELS"]
        = f.text_field :rate, class: "form-control percent", placeholder: "Current Interest Rate *"
        - if @applicant.errors.messages[:rate].present?
          %p.help-block= @applicant.errors.messages[:rate][0]

  .form-group{class: ("has-error" if @applicant.errors.messages[:referred_by].present?)}
    = f.label :email, raw("Referred By (if applicable)"), class: "control-label" if ENV["FORM_LABELS"]
    = f.text_field :referred_by, class: "form-control", placeholder: "Referred By (if applicable)"
    - if @applicant.errors.messages[:referred_by].present?
      %p.help-block= @applicant.errors.messages[:referred_by][0]

  %p.help-block.pull-left * Required
  = f.submit "Submit", class: "btn btn-primary pull-right"